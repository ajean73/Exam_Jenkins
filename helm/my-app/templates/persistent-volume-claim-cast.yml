apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-cast-db-pvc
spec:
  accessModes:
    - {{ .Values.cast_db.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.cast_db.persistence.size }}
