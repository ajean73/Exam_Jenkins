apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-movie-db-pvc
spec:
  accessModes:
    - {{ default "ReadWriteOnce" .Values.movie_db.persistence.accessMode }}
  resources:
    requests:
      storage: {{ default "1Gi" .Values.movie_db.persistence.size }}
